# E2E Testing Environment Variables
# Copy this file to .env.e2e.local and fill in the values
#
# This file is loaded by playwright.config.ts using dotenv.
# The .local suffix ensures it's gitignored (via *.local pattern).
#
# After creating .env.e2e.local, run tests with:
#   npm run test:e2e

# Required for authenticated E2E tests
# Create a GitHub Personal Access Token at https://github.com/settings/tokens
# Scopes needed: read:user, user:email, public_repo
VITE_TEST_GITHUB_TOKEN=ghp_your_token_here

# Supabase configuration (same as your .env)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
